const { primeFactorization } = require('./src/primeFactorization');
const { setOfPrimes } = require('./src/setOfPrimes');

const { isSquare } = require('./src/isSquare');

module.exports = {
  primeFactorization,
  setOfPrimes,
  isSquare,
};
